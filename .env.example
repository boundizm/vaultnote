# PostgreSQL Database URL
# For local development, use a local PostgreSQL instance
# For production, use a cloud provider like Supabase, Railway, or Neon
DATABASE_URL="postgresql://username:password@localhost:5432/vaultnote?schema=public"

# Production example (replace with your actual values):
# DATABASE_URL="postgresql://user:pass@db.example.com:5432/vaultnote?schema=public&sslmode=require"

# Cron job secret (generate a secure random string)
CRON_SECRET="your-secure-random-secret-change-this-in-production"

# Next.js URL (for production deployment)
NEXTAUTH_URL="http://localhost:3000"

# Production deployment notes:
# 1. Copy this file to .env on your server
# 2. Replace DATABASE_URL with your actual PostgreSQL connection string
# 3. Generate a secure random string for CRON_SECRET
# 4. Set NEXTAUTH_URL to your domain (e.g., https://yourdomain.com)
